node_modules
dist
.DS_Store
*.log
.vite

# Local environment — contains API keys, never commit
.env.local
.env.*.local

# Cloud workspace saves (server-side persistence) — never commit user data
data/
